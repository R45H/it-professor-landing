mixin list(data)
	.list
		.list__icon-wrap
			.list__icon(
				style=`background-image: url(${data.icon})`
				role='img'
				aria-label= data.title
			)

		.list__wrap
			if data.title
				.list__title= data.title

			.list__desc= data.desc